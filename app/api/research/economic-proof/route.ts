import{NextRequest,NextResponse}from'next/server';import { bearerSecretMatches } from '@/lib/security/request-auth';import{evaluateEconomicProof}from'@/lib/intelligence/economic-proof';export const runtime='nodejs';export const dynamic='force-dynamic';export async function POST(r:NextRequest){const s=process.env.CRON_SECRET;if(!s||!bearerSecretMatches(r.headers.get('authorization'), s))return NextResponse.json({error:'unauthorized'},{status:401});const x=await evaluateEconomicProof();return NextResponse.json(x,{status:x.ok?200:503});}
