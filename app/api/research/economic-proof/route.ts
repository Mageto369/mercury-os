import{NextRequest,NextResponse}from'next/server';import{evaluateEconomicProof}from'@/lib/intelligence/economic-proof';export const runtime='nodejs';export const dynamic='force-dynamic';export async function POST(r:NextRequest){const s=process.env.CRON_SECRET;if(!s||r.headers.get('authorization')!==`Bearer ${s}`)return NextResponse.json({error:'unauthorized'},{status:401});const x=await evaluateEconomicProof();return NextResponse.json(x,{status:x.ok?200:503});}
